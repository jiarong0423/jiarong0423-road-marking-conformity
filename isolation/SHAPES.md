# 六個形狀

`ERRORS.md` 有 65 條紀錄。它們**不是 65 個不同的錯**,是**六個形狀**在輪。

這份檔案存在的理由:**`ERRORS.md` 一直是輸出,不是輸入。** 我寫進去,
然後動手之前從來不回去讀。所以 E52 修好、E55 在五分鐘後把它寫回去。

**動手之前讀這一頁,不是讀 ERRORS.md。**

---

## 形狀 1:偵測器回報 0,我去替它找外部理由

不先認定偵測器錯,而去主張「現場沒有那個東西」或「照片沒拍到」。

`E10` 宣稱雪佛龍已塗銷,替找不到它的定位器解套 —— 它其實在畫面裡
`E47` §167 是 0,我先說「現場根本沒有 §167」,再說「照片沒拍到那一段」——
      兩個都是推測,都被門牌座標推翻

**檢查**:偵測器回報 0 的時候,**先把原圖打開看**。看不到才談現場。

---

## 形狀 2:修一個缺陷,造出下一個

`E07` 修 E05 時埋的寬鬆預設值
`E20` 修 E01 時犯的同一個「相信區域因為它回傳了遮罩」
`E26` 修 E21 時造出「一個發現說兩項」
`E32` 修 E27 時讓 25/42 直接崩潰
`ER01` **同一次修正裡**把守衛蓋起來又拆掉
`E53` 修取樣點的編輯順手刪掉兩道測試
`E55` 一次復發四個

**檢查**:改完之後,**把改動前後的行為在真實資料上各跑一次比對**,
不是只看新功能有沒有動。

---

## 形狀 3:測試會過,但測不到它宣稱的東西

`E19` 斷言 `encodings == len(QUALITIES)*len(WORK_PX)`,而程式就是這樣算的 —— 恆等式
`E33` 把 Finding 丟給 `_scenario`,而那個判斷在 `assess()` 裡 —— 恆真
`E39` 釘的是措辭不是主張,措辭改對的時候它就壞
`E41` 餵雜訊,而雜訊走提早返回那條路,**到不了**被測的那一行

**檢查**:**每一個新測試都要做突變驗證** —— 把它守的東西改壞,它必須失敗。
不失敗就是空測試。

---

## 形狀 4:守衛寫了,但沒有人會經過它站的那個門

`E07` roi 缺席時回傳 True
`E13` `ROI_FRAC_MAX=0.87` 結構性到不了
`E29` 「未量測」容器在圖、契約、測試三處都不存在
`ER01` `runs=1` 永遠回 True
`E55` 比例判準在目標照片上是不可達程式碼

**檢查**:**證明那條分支會被走到。** 找一個真實輸入讓它觸發,或承認它是死的。

---

## 形狀 5:一個會長出數字的計算,一定會長出數字

`E46` 一堆雜訊取中位數,得到「15.6 公分對法定 20」—— 看起來非常像對的
`E09` Gabor bank 的週期永遠回天花板,28 改成 80 就回 80
`E45`/`E43` 未定種子的分群,同一張照片連續五次給不同答案

**檢查**:報一個數字之前,**問它在元件壞掉的時候會不會長一樣**。
會的話,那個數字沒有資訊。

---

## 形狀 6:把未經查證的東西當成來源

`E25` 把公式除數 155 寫成條號「§155」
`E36` 捏造「新北市道路挖掘作業審查原則」的條文內容,四個詞原文一個都沒有
`E37` 捏造 §165 的條文,而本專案完全沒有 §165 原文
`E49` 把外部 AI 的回答當成 §171 條文引用
`E55` 把既有的誠實註解「**like a** 分向限制線」降級成「§167 **states**」
`E48` 引許可的第一段(最初核准),而當天生效的是第七次變更

**檢查**:寫下任何引號裡的東西之前,`grep` 它。`tests/test_basis_is_sourced.py`
擋 `src/` 的 basis 字串;**文件裡的引文它擋不到**(G39)。

---

## 兩條跨形狀的

**改了共用的東西,只跑最近的那個測試檔就回報「全過」** —— E12、E39,兩次。
**跑全母體會付出代價,也會付出回報** —— EXIF 被砍(E48 前身)就是全 42 張跑完才發現的,
沒有人在找它。
