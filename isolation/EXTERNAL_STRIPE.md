# 外部查證:斜紋方向怎麼算(2026-09-22 晚)—— 候選,不是依據

來源:ASK 通道 `~/.ai_tasks/solutions/20260922_192712_edfbb367.md`。題目附了已失敗的五種做法,要求不重複。

| # | 外部建議 | 狀態 | 打算怎麼驗 |
|---|---|---|---|
| A | **結構張量 / 方向場**(Sobel → J = ⟨Ix²⟩⟨IxIy⟩/⟨IxIy⟩⟨Iy²⟩,高斯視窗 ≈ 斜紋寬;`cv2.cornerEigenValsAndVecs`),做方向直方圖,不抽線段 | OpenCV 官方函式,可驗 | 在自動框出的槽化線多邊形內算方向場;每個斜紋像素一條線,SVD 求消失點,再和邊界束 v_road 算旋轉不變夾角 |
| B | 只有 v_road + K 不夠;要 roll≈0 或 IMU,或用斜紋**等距性**解第二個消失點 | 與本專案已做的「等距反解 v_road」同一個想法 | 手機 EXIF 無 IMU(已查);roll≈0 不可假設(手持) |
| C | **不循環的度量校正**:沿通過 v_road 的線取斜紋交點 xᵢ,擬合 xᵢ = (a·i+b)/(c·i+1),≥4 點最小平方直接解 | 一維射影模型,標準;比我用的 2-D 格點搜尋乾淨 | 換掉 `measure_chevron_phone.py` 的格點搜尋 |
| D | Stephen Se (2000) *Zebra-crossing detection for the partially sighted*(CVPR),§4 用等距白條的幾何級數解地平線;另一篇用斑馬線週期紋理反推外參 | Se 2000 是真實論文(CVPR 2000);第二篇標題待核 | 讀 Se §4,對照 C |

外部答案沒有講到、但我知道的坑:斜紋有**兩臂**,方向場會有兩個峰,消失點要分兩群各算。

實作結果見同日 `DETECTORS.md` 補七。
