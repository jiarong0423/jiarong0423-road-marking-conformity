# 成立的結果(編碼索引)

2026-09-22。這裡**只放自己驗過、有 n、而且知道它不主張什麼**的東西。
沒有編碼的就不是這裡的東西,引用前先看 `docs/claims.md` 和 `isolation/ERRORS.md`。

編碼規則:`V` = verified,兩位數流水號。撤回的不重用號碼。

| 碼 | 是什麼 | n | 證據 | 不主張什麼 |
|---|---|---|---|---|
| **V01** | 兩個地面方向的夾角,`acos((K⁻¹v₁)·(K⁻¹v₂)/…)`,對相機旋轉不變 | 每姿態 5 個,3 個角度 | `tests/test_vanishing_invariance.py`,重跑 sd 6.2e-12–2.2e-11 | 不給長度,不給比例尺。**不需要地平線、俯仰角、相機高度** |
| ~~V02~~ | ~~場景敘事只由當幀量到的發現組成~~ **撤回 2026-09-22** | — | 見 E21、E22 | **它是假的。** 橋、前後順序、水溝蓋、一個裸的 1 秒常數全都是斷言;而且餵純雜訊進去,整段敘事照跑。`_scenario` 現在自己回傳一個 `unmeasured` 清單,等於程式本體承認了這件事 |
| **V03** | 雪佛龍塗銷比例「約三分之二」,用交比,**完全不用相機參數** | 28 條裡的 23 條 | `results/erasure_ratio.json` | 「約三分之二」成立,「64%」不成立:手繪邊界移 ±20 px,中位值 54.9%↔68.3% |
| **V04** | 施工期 A2 傷亡事故 1.00 → 2.62 件/月,全國同期 ×1.04,Poisson p=0.00016 | 12 個月 12 件 vs 8 個月 21 件 | `results/accidents.json` | 單一地點短窗口的**關聯**,不是因果 |
| **V05** | 像素預算:f=2840 px,10 cm 的線在 20 m 處只有 14.2 px;縮到 1000 px 剩 3.47 px | 42,尺寸一致 | `results/pixel_budget.json` | 這是上限不是誤差;沒有比例尺就沒有公分 |
| **V06** | 擾動集成(3 品質 × 3 工作尺寸),不一致就拒答 → 42 張中拒答 38 張 | 42 | `src/marking/situation.py::taper_ensemble` | **拒答是結果**。剩下 4 張報的是界,不是點 |
| **V07** | 把九宮格原始資料給路由模型(而不是彙總計數),信心 0.27 → 0.91 | 見 commit 6162501 | `isolation/src/marking/route.py` | 目前**不在交付路徑上**,它會呼叫模型,放進 `assess()` 會毀掉可重現性 |
| **V08** | 42 張原圖 + 手冊 + 逐張完整性測試(焦距、雜湊、無真實 GPS) | 42 | `tests/test_evidence_integrity.py`,86 項全過 | 不主張影像內容,只主張檔案沒被動過 |
| **V09** | **把偵測結果畫在原照片上逐張看** | — | `src/marking/draw.py`,commit 3b91a48 | 人眼判讀,判讀者是主代理。**今天所有的發現都來自這件事**,見 E11 |
| **V10** | duty 估計器 + 保留式諧波檢定(3/4/5 次諧波沒參與估計) | 合成校正,每情境 1 條 | `isolation/scripts/stage5_predicate.py` | 方波 0.004–0.045,正弦 0.101–0.178,雜訊 1.589;重雜訊方波會與正弦重疊 |
| **V11** | `road_region()`:**一條聲明的橫線**(畫面高 40% 以下),外加約 2% 的修邊 | 42 | 見 E20:保留切線以下的中位 **98.0%**,佔全畫面 55.6–60.0%(純橫帶是 60.0%) | **不是車道偵測器。** 宣稱的四層裡只有位置那層在作用(E20)。它唯一做到的是**拒絕天空**,而那擋掉了兩個已確認的 §167 偽陽性 |

## 這裡沒有的

- §167 雙白線:**0/42**。出現過的九個判定都畫出來看過,全是屋簷、圍籬浪板、材質交界、雙黃線、白配黃。見 E05。
- §171 雪佛龍:斜紋線**在畫面裡**(F07 原解析度可見),但沒有定位器找得到它。見 E09、E10。
- 任何公分級數字:沒有比例尺,`scale_source = NONE`,全部拒答。
- 紅線 31/42、圍籬 9/42、收窄 10/42、補丁 34/42:這些是**換掉 `carriageway()` 之前**的數字,尚未重跑,現在不能引用。
